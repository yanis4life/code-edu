Build a complete gamified programming education platform specifically designed for deployment on Cloudflare (Cloudflare Pages + Workers + D1 database + R2 object storage + Queues).

Platform Architecture:
- Backend API: Node.js (Express) running on Cloudflare Workers or Pages Functions
- Background Processing: Python microservices for code analysis, PDF generation, and data processing (deployed as separate Workers)
- Database: Cloudflare D1 (SQLite-based) with proper schema design
- File Storage: Cloudflare R2 for all user-uploaded content
- Frontend: Vanilla HTML, CSS, JavaScript (no frameworks, no libraries, no CDN dependencies except for syntax highlighting)
- Code execution: Sandboxed iframe for HTML, CSS, JavaScript ONLY (client-side execution)

Core Features (ALL must be fully functional, no placeholders):
1. User System: Registration, login, profile pages, progress tracking (XP, levels, badges), leaderboards (global & friends), profile avatars (upload to R2 with automatic resizing and optimization), bio, account settings, avatar management (upload, crop, delete)

2. Dashboard: Current courses overview, daily challenge with streak counter, recommended next lesson, global ranking position, quick stats (total XP, lessons completed, streak, level)

3. 1 Programming Language : Python
   - language levels : 100 levels from easy to hard
   - Each level includes: theory text, code examples, practical challenge (fix bug, complete code, predict output, drag-and-drop, true or false)
   - Code editor: syntax highlighting, line numbers, auto-indent, run button, output display
   - For HTML/CSS/JS: output via sandboxed iframe
   - For others: simulated output or test results display

4. Game Mechanics:
   - XP earned from lessons, challenges, daily streaks
   - Level unlock requires passing current level tests
   - Timed coding duels with simulated opponent
   - Achievements (First Code, 10 Challenges, Polyglot, Master, etc.)
   - Skill tree visualization with language mastery progression

5. Social Features:
   - Public profiles with shared solutions (code snippets stored in R2)
   - Comment and rate solutions
   - Friend system with friend requests
   - Private messaging between users (attachments stored in R2)

6. Admin Panel:
   - Add/edit lessons, challenges, test cases
   - View user statistics
   - Ban/unban users
   - Reset user progress
   - Manage R2 storage (view usage, cleanup orphaned files)
   - Monitor Worker performance and Queues

7. Database Schema: Tables for users, progress, lessons, challenges, submissions, friendships, messages, achievements, badges

8. UI/UX:
   - Dark/light theme toggle (persisted)
   - Fully responsive (mobile, tablet, desktop)
   - Modern design with subtle CSS animations
   - Collapsible sidebar with language list

9. Additional Features:
   - Code snippets library (save favorite solutions, stored in R2 with metadata)
   - Weekly coding contests with rankings
   - Hints system (costs XP)
   - Audio feedback for correct/incorrect answers (Web Audio API)
   - Export progress as PDF resume (generated by Python microservice, stored in R2 with signed URLs)
   - Search bar (lessons, challenges, users) using D1 full-text search
   - Notification system (friend requests, contest results, streak reminders) using Cloudflare Queues
   - Landing page with platform overview (static HTML)

R2 Storage Implementation Details:
- All user-uploaded avatars stored in R2 bucket with public or signed URLs
- Generate unique filenames using UUID to prevent collisions
- Implement image optimization (resize, compress) before uploading to R2
- Store file metadata (original name, size, mime type, upload date) in database
- Implement file deletion when users remove avatars or content
- Use presigned URLs for temporary access to private files (user exports, attachments)
- Configure CORS policies for R2 bucket to allow direct uploads from frontend
- Implement automatic cleanup of unused files using Cloudflare Queues (cron triggers)
- Set up R2 bucket lifecycle rules for automatic expiration of temporary files
- Use environment variables for R2 credentials

Backend Architecture Details:

Node.js (Primary API):
- Express.js with MVC structure (Models, Controllers, Routes)
- JWT-based authentication with refresh tokens
- Input validation using Joi or similar
- Rate limiting per user/IP
- D1 database connection pooling
- R2 file operations (upload, delete, generate signed URLs)
- Real-time notifications via Server-Sent Events or WebSockets
- API versioning for future compatibility
- Structured error handling with proper HTTP status codes
- Request logging for debugging (Cloudflare Logpush)

Python (Background Services):
- Deployed as separate Cloudflare Workers with Python support
- Code analysis service (parse submissions, detect patterns)
- PDF generation service (user progress reports)
- Data analytics service (generate insights, recommendations)
- Achievement detection engine (process user actions, award badges)
- Leaderboard update service (periodic recalculation)
- Email notification service (daily reminders, contest alerts)
- Image processing service (avatar optimization)
- Code snippet validation service (security scanning)

Cloudflare Queues Integration:
- Process user achievements asynchronously
- Handle email notifications in background
- Process PDF generation requests
- Update leaderboards periodically
- Clean up temporary R2 files
- Process user data exports
- Generate weekly contest results
- Handle friend request notifications
- Process comment notifications

Technical Requirements:
- All Node.js code must follow MVC structure with clear separation
- Python code must be modular with clear function separation
- Database queries must use prepared statements for D1 (SQL injection protection)
- All user input must be properly sanitized (XSS protection)
- R2 uploads must validate file types, sizes, and scan for malicious content
- No code comments in final production files (clean code with self-documenting naming only)
- No emojis anywhere in code, output, or UI
- Performance optimized: minimize database queries, use caching where appropriate
- Error handling must be graceful with user-friendly messages
- Implement proper file upload error handling (size limits, type validation, network issues)
- Use environment variables for all configuration (no hardcoded values)
- Implement health check endpoints for all services
- Use structured logging (Winston or Pino)

Security Requirements:
- JWT tokens with short expiration (15 minutes access, 7 days refresh)
- Password hashing using bcrypt (cost factor 12)
- CSRF protection for state-changing operations
- CORS properly configured for frontend domain
- Helmet.js for security headers
- Input sanitization using DOMPurify for user content
- Rate limiting per endpoint (100 requests/minute for auth, 1000 for reading)
- File upload limits: 5MB for avatars, 10MB for code snippets, 20MB for attachments
- Secure cookie flags (HttpOnly, Secure, SameSite=Strict)

Cloudflare-Specific Deployment:
- Configure Cloudflare Pages for static frontend hosting
- Deploy Node.js API as Cloudflare Workers or Pages Functions
- Deploy Python services as separate Workers
- Use D1 as primary database with proper migrations
- Use R2 for all persistent file storage
- Use Queues for background job processing
- Environment variables for sensitive data
- Static assets (CSS, JS, images) served from Pages
- Dynamic routes handled by Workers/Functions
- Proper CORS and security headers configured
- Edge-compatible code with fast cold start times
- Configure R2 bucket with appropriate privacy settings (public for avatars, private for exports)
- Use Worker KV for caching frequently accessed data (leaderboards, lessons)
- Set up Cron Triggers for scheduled tasks (daily leaderboard updates, weekly contests)
- Configure Cloudflare Access for admin panel protection

Deliverables:
1. Complete Node.js source code (MVC structure, all routes, controllers, models)
2. Complete Python source code (all microservices)
3. Frontend HTML, CSS, JavaScript files
4. Database schema as SQL dump (compatible with D1)
5. R2 bucket configuration and setup instructions
6. Cloudflare Queues configuration and consumer setup
7. Step-by-step deployment instructions for Cloudflare (Pages, Workers, D1, R2, Queues)
8. Production-ready secure code with all security measures applied
9. Fully functional platform with ALL features working (no TODOs, no placeholders)
10. Environment variables template with all required keys
11. Dependency manifests for Node.js and Python
12. Cloudflare configuration file
13. API documentation (OpenAPI/Swagger)

Important: Build the FULL platform with ALL features listed above functional. Every level, challenge, game mechanic, and social feature must work. R2 storage must be fully integrated for all user content. Node.js API must be fully functional with all endpoints. Python services must handle background processing. No mock data, no simulated features. This is a production-ready system.

use foldring paths without app/views/ or app/models but you can use /api /login/ or like this